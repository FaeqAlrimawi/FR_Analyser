package Testing;

import javax.xml.parsers.ParserConfigurationException;

import bigraphspace.model.BasicSignature;
import bigraphspace.model.Control;
import bigraphspace.model.Control.Status;

import bigraphspace.model.signaturexml.DefinitionException;
import bigraphspace.model.signaturexml.Definitions;
import bigraphspace.model.signaturexml.SignatureFactory;
import bigraphspace.model.xml.*;

public class TestBigraphSpace {

	
	public static void main(String []args) {
		
		BasicSignature sig;
		
			
			/*Definitions defs = new Definitions();
			//Controls cl = new Controls();
			Control c1 = new Control("Name", "description", null, Status.active, 0, null);
			
			cl.getControl().add(c1);
			
			sig = SignatureFactory.createSignature(null);
			DomBigraph t = new DomBigraph(sig);
			
			DomMatcher mt = new DomMatcher();*/
	
		
		
	}
}
